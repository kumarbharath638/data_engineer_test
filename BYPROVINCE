CREATE TABLE AS "INTERVIEW_DB"."PLAYGROUND_BHARATH_KUMAR"."BYPROVINCE"
AS

SELECT LATITUDE, LONGITUDE, PROVINCE,
SUM(TOTAL_ACTIVE_CASES) AS TOTAL_ACTIVE_CASES,
SUM(TOTAL_RECOVERED) AS TOTAL_RECOVERED,
SUM(TOTAL_DEATHS) AS TOTAL_DEATHS
FROM "INTERVIEW_DB"."PLAYGROUND_BHARATH_KUMAR"."COVIDDATA"
GROUP BY LATITUDE, LONGITUDE, PROVINCE
ORDER BY PROVINCE
